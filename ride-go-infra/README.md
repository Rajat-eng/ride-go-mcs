# ride-go-infra

Infrastructure and deployment repository for ride-go services.

## Layout

- `terraform/modules`: reusable infrastructure modules
- `terraform/environments`: environment-specific stacks
- `helm`: application deployment charts
