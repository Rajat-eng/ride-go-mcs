replicaCount: 1
mode: daemonset
resources:
  requests:
    cpu: 100m
    memory: 200Mi
  limits:
    cpu: 250m
    memory: 400Mi
config:
  processors:
    memory_limiter:
      check_interval: 5s
      limit_mib: 256
      spike_limit_mib: 64
