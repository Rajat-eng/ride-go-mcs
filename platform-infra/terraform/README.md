# Terraform Modules

Each folder is a minimal module stub. Replace placeholder values and add providers, variables, and outputs as needed.

Current module folders:

- `networking`
- `eks`
- `rds`
- `rabbitmq`
